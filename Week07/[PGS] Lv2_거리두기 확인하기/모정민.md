# [Lv 2] 거리두기 확인하기

[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/81302)

`#BFS` `#그래프탐색` `#구현` `#완전탐색`

### 제출 일자
2026년 3월 26일 22.64ms

### 문제 설명 (요약)
코로나19 방역 수칙에 따라 5x5 크기의 대기실 5개에서 응시자들이 거리두기를 지키고 있는지 확인한다.
대기실은 다음 문자로 구성된다.

- **P** : 응시자가 앉아있는 자리
- **O** : 빈 테이블
- **X** : 파티션 (벽)

응시자들 끼리는 **맨해튼 거리 2 이하**로 앉을 수 없다.
단, 두 응시자 사이에 파티션(`X`)으로 막혀 있는 경우는 허용된다.
- 맨해튼 거리가 1인 경우: 무조건 거리두기 위반
- 맨해튼 거리가 2인 경우: 경로 사이에 빈 테이블(`O`)이 있다면 위반, 파티션(`X`)이 있다면 통과

위 규칙을 바탕으로 각 대기실별로 모든 응시자가 거리두기를 지키고 있는지 판별한다.

### 입력
- places: 5개의 대기실 정보를 담은 2차원 문자열 배열 (5x5 크기의 문자열이 5개)

### 출력
각 대기실별로 거리두기를 지키고 있으면 `1`, 한 명이라도 지키지 않고 있으면 `0`을 담은 1차원 정수 배열 반환 (크기 5)

---

### 핵심 풀이 전략
1. 방향 전환을 위한 moveY[], moveX[] 선언
2. places를 돌며 P가 있다면 bfs() 실행
3. 거리 2이내 P가 있는지 조기 확인 후 있다면 false 없다면 그대로 진행
4. 4방향으로 이동한 후 X가 아니라면 que에 삽입
5. 큐가 빌 때까지 반복한 후 true 반환
6. bfs를 통해 false가 반환되면 answer에 0, true라면 1

---

### Java 구현 코드

```java
import java.util.*;

class Solution {
    
    int[] moveY = {0, 0, 1, -1};
    int[] moveX = {1, -1, 0, 0};
    
    public int[] solution(String[][] places) {
        int[] answer = new int[5];
        
        for(int n = 0; n < 5; n++){
            
            boolean check = false;
            outer: for(int i = 0; i < 5; i++){
                for(int j = 0; j < 5; j++){
                    if(places[n][i].charAt(j) == 'P' && !bfs(places, n, i, j)){
                        answer[n] = 0;
                        check = true;
                        break outer;
                    }
                }
            }
            if(!check) answer[n] = 1;
        }
        return answer;
    }
    
    public boolean bfs(String[][] places, int n, int y, int x){
        
        Deque<int[]> que = new ArrayDeque<>();
        boolean[][] visited = new boolean[5][5];
        que.offer(new int[]{y, x, 0});
        visited[y][x] = true;
        
        while(!que.isEmpty()){
            
            int[] poll = que.poll();
            int curY = poll[0];
            int curX = poll[1];
            int curCount = poll[2];
            
            // 최대 이동거리 2이내에 P가 있다면 false 반환
            if((curCount == 1 || curCount == 2) && places[n][curY].charAt(curX) == 'P') return false;
            
            if(curCount >= 2) continue;
            
            for(int i =0; i < 4; i++){
                int nextY = curY + moveY[i];
                int nextX = curX + moveX[i];
                
                if(nextY >= 0 && nextY < 5 && nextX >= 0 && nextX < 5
                  && places[n][nextY].charAt(nextX) != 'X' && !visited[nextY][nextX]){
                    que.offer(new int[]{nextY, nextX, curCount+1});
                    visited[nextY][nextX] = true;
                }
            }
        }
        return true;
    }
    
    
}

```

### 삽질 기록 🧠
1. 처음에 거리가 2 이내여야하는데 2일 때만 체크하도록 함. 1일 때도 조기 종료가 될 수 있으므로 1 or 2 일 때 P인지 조기 확인 하도록 변경
2. nextY <= 5 잘못된 조건문 처리

<br>

---

### 배운 점 && 보완할 점
1. 코드가 매우 지저분한데, X인지 아닌지 판별하는 것도 따로 빼소 continue로 처리할 수 있을 것 같다.
