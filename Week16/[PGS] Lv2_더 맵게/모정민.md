# [Level 2] 더 맵게

[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/42626)

`#힙(Heap)` `#자료구조`

### 제출 일자
2026년 6월 1일 1.44ms

### 문제 설명 (요약)
모든 음식의 스코빌 지수를 `K` 이상으로 만들기 위해, 스코빌 지수가 가장 낮은 두 개의 음식을 섞어 새로운 음식을 만듭니다.
- 섞은 음식의 스코빌 지수 = 가장 맵지 않은 음식의 스코빌 지수 + (두 번째로 맵지 않은 음식의 스코빌 지수 * 2)
- 모든 음식의 스코빌 지수가 `K` 이상이 될 때까지 반복하여 섞은 횟수의 최솟값을 반환합니다. 모든 음식을 섞어도 `K` 이상 만들 수 없다면 `-1`을 반환합니다.

---

### 핵심 풀이 전략
1. **최소 힙(Min Heap) 활용**: 매번 배열에서 가장 작은 값 두 개를 찾아야 합니다. 일반 정렬을 매번 수행하면 시간 초과가 발생하므로, 삽입과 삭제가 $O(\log N)$인 우선순위 큐(`PriorityQueue`)를 사용합니다.
2. **종료 조건 확인**: 큐의 최상단 값(`peek()`)이 `K` 이상이면 모든 음식이 `K` 이상이 된 것이므로 반복을 종료합니다.
3. **예외 처리**: 큐에 남은 원소가 1개인데 그 값이 `K`보다 작다면 더 이상 섞을 수 없으므로 `-1`을 반환합니다.

---

### 내가 구현한 구현 코드
```java
import java.util.*;

class Solution {
    public int solution(int[] scoville, int K) {
        int answer = 0;
        
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        
        for(int s : scoville){
            pq.offer(s);
        }
        
        while(true){
            
            if(pq.peek() >= K) break;
            
            if(pq.size() >= 2){
                int first = pq.poll();
                int second = pq.poll();
                
                int newScoville = first + (second * 2);
            
                pq.offer(newScoville);
                
                answer++;
            }else{
                return -1;
            }
            
        }
        
        return answer;
    }
}


### 회고 및 삽질기록 🧠


### 배운 점 && 보완할 점
